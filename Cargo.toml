[package]
name = "redisk"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
lru = "0.12"
bytes = "1.5"
bincode = "1.3"

[dev-dependencies]
tempfile = "3"
